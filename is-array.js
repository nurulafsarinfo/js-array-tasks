const destinations = ['rangamati', 'shilet', 'japlong'];

const books = ['python', 'javascript', 'c++', 'php'];

const fruits = [];

const tree = "Mango tree Apple tree";

const num = 12455623;

console.log("Is this a 'Array': "+Array.isArray(destinations))
console.log("Is fruits a 'Array': "+Array.isArray(fruits))
console.log("Is this tree a 'Array': "+Array.isArray(tree))
console.log("Is this a 'Array': "+Array.isArray(books))